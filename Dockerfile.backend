FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies including nodemon for development
RUN npm install --include=dev && npm install -g nodemon cross-env

# Expose port
EXPOSE 3000

# Command for development with hot reload
CMD ["npm", "run", "dev"]
